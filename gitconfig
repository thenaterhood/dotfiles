[alias]
    fa = fetch --all
    last = log -1 HEAD
    amend = commit --amend
    uncommit = reset HEAD~1
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    co = checkout
    cob = checkout -b
    cm = commit -m
    cam = commit -am
    s = status
